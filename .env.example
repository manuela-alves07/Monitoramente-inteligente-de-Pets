# Copie este arquivo para .env e preencha (nao commite o .env)

# Neon (nuvem) — cole a connection string ENTRE ASPAS (por causa do & na URL):
DATABASE_URL="postgresql://usuario:senha@ep-xxxxx-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require"

# PostgreSQL local (pgAdmin) — use so se NAO tiver DATABASE_URL acima:
# PG_HOST=localhost
# PG_PORT=5432
# PG_DB=monitoramento_pets
# PG_USER=postgres
# PG_PASSWORD=sua_senha_aqui
